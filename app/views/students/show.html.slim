section#student
  h1
    = @student.full_name
  .img-wrapper
    = image_tag avatar_image(@student.avatar), alt: "#{@student.first_name}-image", class: '.student-img'

  - if @user.owns_page? params[:id]
    = render 'forms/student_editor'

  - else
    = render 'visitors/students_show'
